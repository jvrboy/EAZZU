# Deriv Scalper Bot Dependencies
# Python 3.8+

# Core dependencies
websockets>=10.0
asyncio-throttle>=1.0.2

# Optional: for enhanced logging
colorlog>=6.7.0

# For data analysis (optional)
numpy>=1.21.0
pandas>=1.3.0

# For GUI (included in Python standard library, but tkinter-ppp may be needed on some systems)
# tkinter is usually included with Python on Windows and macOS
# On Linux: sudo apt-get install python3-tk

# Testing
pytest>=7.0.0
pytest-asyncio>=0.18.0

# Packaging
setuptools>=45
wheel>=0.37
